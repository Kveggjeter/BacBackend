#!/bin/bash

echo "God natt google mf"
pkill -TERM -x google-chrome google-chrome-stable chrome chromedriver 2>/dev/null

sleep 2
pkill -KILL -x google-chrome google-chrome-stable chrome chromedriver 2>/dev/null

echo "Finito"
